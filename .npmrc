# npm 설정으로 설치 시 크기 줄이기

# 프로덕션 설치 시 devDependencies 제외
production=true

# 선택적 종속성 설치 안함
omit=optional

# 패키지 설치 시 펀딩 메시지 숨기기
fund=false

# 패키지 설치 시 감사 실행 안함 (속도 향상)
audit=false

# 진행률 표시 안함 (CI/CD용)
progress=false

# 패키지 잠금 파일 사용
package-lock=true

# 엄격한 SSL 사용 (보안)
strict-ssl=true

# 캐시 최소화
prefer-offline=true

# 로그 레벨 (에러만)
loglevel=error